{
  "name": "neverranked-dashboard",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply neverranked-app --remote",
    "db:migrate:local": "wrangler d1 migrations apply neverranked-app --local",
    "test": "tsx --test test/*.test.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250410.0",
    "tsx": "^4.21.0",
    "wrangler": "^4.81.1"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.1.0"
  }
}
