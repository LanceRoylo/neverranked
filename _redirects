# Cloudflare Workers Static Assets _redirects.
#
# SITE IN REBUILD (2026-05-19): full-site holder. Every path serves
# the holder at /index.html. The old marketing pages are also pruned
# out of dist/ in the deploy step, so this is belt-and-suspenders.
# The /check redirect and all marketing routes are intentionally
# down during the rebuild. The instrument (app.neverranked.com) is a
# separate Worker and is unaffected. To restore the old site: revert
# this file + index.html, restore the old build, run a normal build.
/*    /index.html    200
