# Technical Audit — Bank of Hawaii

**Auditor:** Never Ranked
**Sample date:** 2026-05-08
**Pages sampled:** 9 representative pages across homepage, timeline/history, annual report, service pages, blog, team profiles, careers, community news

---

## Summary

Bank of Hawaii's site is content-rich and visually polished, with proper Open Graph social cards deployed across all 9 sampled pages. The foundation is compromised by missing canonicals (100% of sampled pages), inconsistent H1 structure (3 pages have none, 2 have multiples), and sparse schema deployment outside a few key pages. The site has testimonial and rating language on 3 pages but no structured AggregateRating markup, meaning AI engines have no machine-readable hook to cite customer satisfaction.

**Overall grade: C** (good content and social layer, serious gaps in canonical discipline and schema coverage)

---

## Findings

### 1. Make sure Google and AI engines see the right version of each page ⚠ HIGH PRIORITY

Every page in your site is missing a canonical tag (9 of 9 sampled pages). The canonical tag tells Google, ChatGPT, Perplexity, and other crawlers which URL is the "official" version when duplicates or near-duplicates exist (query parameters, tracking codes, www vs non-www, http vs https). Without it, you risk diluted link equity, indexing confusion, and fragmented citation in AI answer engines. Even if you think you have no duplicates, the tag acts as insurance and is a best practice signal.

**Impact:** Search engines may split your page authority across multiple URLs. AI engines may cite a variant URL instead of your preferred one, or skip the page entirely if they detect ambiguity.

**Fix:**

- **WordPress (Yoast SEO or Rank Math):** Canonical is auto-generated by default. Check Settings → Permalinks and confirm your Preferred Domain is set. Verify the plugin is active and canonical output is enabled in Yoast → Search Appearance → General or Rank Math → Titles & Meta.
- **Squarespace:** Canonical tags are auto-added. If missing, check Settings → Advanced → Code Injection to ensure no custom code is stripping them.
- **Webflow:** Canonical is auto-generated. Verify in Page Settings → SEO Settings that you haven't overridden it with a custom value pointing elsewhere.
- **Wix:** Canonical tags are automatically added. Confirm in Site SEO settings that your primary domain is correctly configured.
- **Custom-coded or enterprise CMS:** Add this to the `<head>` of every page, with `CANONICAL_URL` replaced by the live, published URL of that page:

```html
<link rel="canonical" href="https://www.boh.com/CANONICAL_URL">
```

For your homepage (`https://www.boh.com`):

```html
<link rel="canonical" href="https://www.boh.com">
```

For the timeline page (`https://www.boh.com/125/timeline`):

```html
<link rel="canonical" href="https://www.boh.com/125/timeline">
```

**Technical detail:** The `rel="canonical"` link element must appear in the `<head>` section, must use an absolute URL (include the `https://` and domain), and should point to the URL you want indexed. Google's documentation: developers.google.com/search/docs/crawling-indexing/consolidate-duplicate-urls.

---

### 2. Let AI engines cite your customer proof ⚠ HIGH PRIORITY

We found testimonial language and rating references on 3 of your pages (`/125/timeline`, `/about-us/annual-report-2024/year-in-review`, and the same annual report page), but zero AggregateRating schema across the entire site. When ChatGPT, Perplexity, or Google's AI Overviews evaluate "best bank in Hawaii" or "most trusted Hawaii financial institution," they look for structured ratings (star count, review count, rating value). Without this markup, they have no machine-readable social proof to cite, even though your prose mentions customer satisfaction.

**Impact:** AI engines will favor competitors with structured rating data when answering queries about reputation, trust, or "best" comparisons. You lose the credibility lift that comes from being cited with a star count.

**Fix (NeverRanked schema deployment):**

We will deploy AggregateRating schema via our snippet for the pages where you have rating or testimonial evidence. This does not require you to edit code.

**Fix (if you want to add it manually before our deployment):**

Identify 2-3 pages where you have verifiable customer ratings (Google Business Profile, Trustpilot, internal NPS data, etc.). Add this JSON-LD block to the `<head>` or end of `<body>`, replacing placeholders with your actual figures:

```json
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "BankOrCreditUnion",
  "name": "Bank of Hawaii",
  "aggregateRating": {
    "@type": "AggregateRating",
    "ratingValue": "4.6",
    "reviewCount": "487"
  }
}
</script>
```

**Technical detail:** The `ratingValue` must be on a 1-5 or 1-10 scale and must reflect real customer data (Google's policies prohibit fabricated ratings). The `reviewCount` must be an integer. This schema is part of the schema.org `Organization` vocabulary. Validation tool: validator.schema.org.

---

### 3. Fix heading structure on 5 of 9 pages ⚠ MEDIUM PRIORITY

3 pages have no H1 tag at all (`/about-us/annual-report-2024/year-in-review`, `/bank-by-appointment`, `/community/olelo`). 2 pages have multiple H1s (`https://www.boh.com` with 4, `/careers/benefits` with 2). The H1 is the primary semantic heading that tells search engines and screen readers what the page is about. Pages without an H1 are harder for crawlers to categorize. Pages with multiple H1s dilute the topical signal.

**Impact:** Google may struggle to understand the primary topic of pages missing an H1. AI engines may skip these pages when they lack a clear topical anchor. Accessibility suffers (screen reader users navigate by heading hierarchy).

**Fix:**

Each page should have exactly one H1, and it should be the main headline that describes the page's purpose.

**Pages missing H1:**

| Page URL | Current state | Recommended H1 |
|----------|---------------|----------------|
| `/about-us/annual-report-2024/year-in-review` | No H1 (first heading is H2) | "Year in Review" or "Bank of Hawaii 2024 Year in Review" |
| `/bank-by-appointment` | No H1 (first heading is H2) | "Bank by Appointment" |
| `/community/olelo` | No H1 (first heading is H2) | "ʻŌlelo Hawaiʻi at Bank of Hawaii" |

**Pages with multiple H1s:**

| Page URL | Current H1 count | Fix |
|----------|------------------|-----|
| `https://www.boh.com` | 4 | Keep "Earn up to 5x more than the average savings rate" as the H1. Downgrade the other 3 to H2. |
| `/careers/benefits` | 2 | Keep "Taking care of you." as the H1. Downgrade the second to H2. |

**How to fix:**

- **WordPress (block editor):** Click the heading block, use the toolbar to change "Heading 1" to "Heading 2" (or vice versa).
- **WordPress (classic editor):** Highlight the heading text, use the Format dropdown to select "Heading 1" or "Heading 2".
- **Squarespace / Webflow / Wix:** Click the heading, use the text style dropdown to select "Heading 1" or "Heading 2".
- **Custom-coded:** Change `<h2>` to `<h1>` (or `<h1>` to `<h2>`) in your HTML template.

**Technical detail:** HTML5 allows multiple H1s per page, but search engines and accessibility guidelines still favor the single-H1 pattern for clarity. The first H1 should appear early in the DOM and match or closely paraphrase the `<title>` tag.

---

### 4. Trim 3 meta descriptions and 1 title to avoid truncation ⚠ LOW PRIORITY

3 pages have meta descriptions over 160 characters, which will be cut off in search results. 1 page has a title over 65 characters, which will also truncate.

**Impact:** Truncated titles and descriptions reduce click-through rate because the searcher can't see your full value proposition. The cut-off text may land mid-sentence.

**Affected pages:**

| Page URL | Element | Current length | Issue |
|----------|---------|----------------|-------|
| `/125/timeline` | meta description | 181 chars | Will truncate after ~160 |
| `/about-us/annual-report-2024/year-in-review` | meta description | 198 chars | Will truncate after ~160 |
| `/community/olelo` | meta description | 166 chars | Will truncate after ~160 |
| `/business/center-for-family-business-and-entrepreneurs/experts/roger-khlopin` | title | 79 chars | Will truncate after ~65 |

**Fix:**

Edit the meta description and title tag for each affected page to fit within the limits. Keep the core value proposition up front.

**Example revision for `/125/timeline`:**

- **Current (181 chars):** "For 125 years, we've been a bank with a single focus — our unwavering commitment to doing what's right for our customers and our communities — see our story."
- **Revised (157 chars):** "For 125 years, Bank of Hawaii has been committed to doing what's right for customers and communities. Explore our story of innovation and service."

**Example revision for `/business/center-for-family-business-and-entrepreneurs/experts/roger-khlopin`:**

- **Current (79 chars):** "Roger Khlopin - Center for Family Business & Entrepreneurs - Bank of Hawaii"
- **Revised (63 chars):** "Roger Khlopin, CFA - Family Business Advisor - Bank of Hawaii"

**How to edit:**

- **WordPress (Yoast or Rank Math):** Edit the page, scroll to the Yoast/Rank Math panel below the editor, edit the "SEO title" and "Meta description" fields.
- **Squarespace:** Pages → click the page → click the gear icon → SEO → edit "Page Title" and "Description".
- **Webflow:** Pages panel → click the page → Settings tab → SEO Settings → edit "Title Tag" and "Meta Description".
- **Wix:** Pages menu → click the page → click the three-dot menu → SEO (Google) → edit "Page title" and "Page description".
- **Custom-coded:** Edit the `<title>` and `<meta name="description" content="...">` tags in your HTML `<head>`.

**Technical detail:** Google's desktop SERP displays approximately 60-65 characters for titles and 155-160 characters for descriptions. Mobile truncates slightly earlier. Yoast and Rank Math show live pixel-width previews.

---

### 5. Add alt text to 10 images across 9 pages ⚠ MEDIUM PRIORITY

10 of 140 images (7%) are missing alt attributes. Alt text serves two purposes: accessibility for visually impaired users (screen readers speak the alt text aloud) and a fallback signal for search engines when they can't interpret the image content directly.

**Impact:** Screen reader users lose context. Google Images can't index the images properly. AI engines have less context when interpreting your pages (they use alt text to understand what the image illustrates).

**Affected pages:**

| Page URL | Images missing alt |
|----------|-------------------|
| `https://www.boh.com` | 1 |
| `/125/timeline` | 1 |
| `/about-us/annual-report-2024/year-in-review` | 1 |
| `/bank-by-appointment` | 1 |
| `/blog/what-to-do-if-you-receive-a-financial-windfall` | 1 |
| `/business/center-for-family-business-and-entrepreneurs/experts/roger-khlopin` | 2 |
| `/careers/benefits` | 1 |
| `/community/olelo` | 1 |
| `/community-news/community-livekokua-bookmusic` | 1 |

**Fix:**

Go to each page, identify the image missing alt text (check the HTML or use a browser inspector), and add a short descriptive phrase.

**How to add alt text:**

- **WordPress (block editor):** Click the image block → right sidebar → "Alt text (alternative text)" field → type a description.
- **WordPress (media library):** Media → Library → click the image → "Alternative Text" field.
- **Squarespace:** Click the image block → Edit → Design tab → "Alt Text" field.
- **Webflow:** Select the image element → Settings panel (right sidebar) → "Alt Text" field.
- **Wix:** Click the image �� Settings → "What's in this image? Describe it for SEO and accessibility" field.
- **Custom-coded:** Add or edit the `alt=""` attribute in your `<img>` tag:

```html
<img src="/images/branch-photo.jpg" alt="Bank of Hawaii branch lobby in Honolulu">
```

**Best practices for alt text:**

- Describe what the image shows in 5-15 words.
- Don't start with "Image of" or "Photo of" (screen readers already announce "image").
- For decorative images with no informational content, use an empty alt attribute (`alt=""`) so screen readers skip them.
- For charts or graphs, describe the insight (e.g., "Bar chart showing 15% increase in mobile banking adoption from 2023 to 2024").

**Technical detail:** The `alt` attribute is part of the HTML `<img>` element spec. WCAG 2.1 Level A compliance requires all informational images to have meaningful alt text. Google's image search uses alt text as a primary ranking signal.

---

### 6. H1 structure — ⚠ ISSUES

Covered in Finding #3 above. 3 pages have no H1, 2 pages have multiple H1s. 4 pages have a clean single-H1 structure.

---

### 7. Image alt text — ⚠ ISSUES

Covered in Finding #5 above. 10 of 140 images (7%) are missing alt text. This is a small percentage but should be brought to zero for accessibility and AI engine context.

---

### 8. Word counts ��� ✓ HEALTHY

All 9 sampled pages exceed 300 words. No thin content detected. Strong depth across the board.

| Page | Word count | Status |
|------|-----------|--------|
| `https://www.boh.com` | 1,354 | ✓ Substantial |
| `/125/timeline` | 481 | ✓ Sufficient |
| `/about-us/annual-report-2024/year-in-review` | 2,257 | ✓ Deep |
| `/bank-by-appointment` | 1,744 | ✓ Deep |
| `/blog/what-to-do-if-you-receive-a-financial-windfall` | 1,227 | ✓ Substantial |
| `/business/center-for-family-business-and-entrepreneurs/experts/roger-khlopin` | 883 | ✓ Sufficient |
| `/careers/benefits` | 1,004 | ✓ Substantial |
| `/community/olelo` | 1,188 | ✓ Substantial |
| `/community-news/community-livekokua-bookmusic` | 752 | ✓ Sufficient |

---

## Priority list (technical)

| # | Fix | Impact | Effort |
|---|-----|--------|--------|
| 1 | Add canonical tags to all pages | HIGH | LOW (template-level fix) |
| 2 | Deploy AggregateRating schema on 2-3 pages with customer proof |