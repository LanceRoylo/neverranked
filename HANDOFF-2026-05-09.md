# Window Handoff — 2026-05-09 evening HST

This document is a handoff from the other Claude Code window working on
the same `/Users/lanceroylo/Desktop/neverranked/` repository. Read this
fully before making any changes so we do not collide.

## What just shipped (from the other window)

Major work landed in the last 24 hours. Do NOT redo or revert any of this:

### MCP server published
- `@neverranked/mcp@0.1.1` is live on npm.
- Listed in the official MCP registry as `io.github.LanceRoylo/mcp`.
- All work in `mcp-server/` is current. **Do not bump the version
  unless explicitly building a 0.1.2 / 0.2.0 patch.**

### Blog post live
- `/blog/the-first-aeo-mcp-server/` is live in production.
- Linked from `/blog/index.html` and `sitemap.xml`.
- **Do not modify the blog post content** without coordinating.

### Social assets ready (need scheduling, not editing)
- `social/posts/2026-05-08-claude-hotel-misspell/` — Halekulani-pivot
  card + captions. Personal LinkedIn post already published. Same
  card slated for Instagram company page.
- `social/posts/2026-05-09-mcp-launch/` — MCP launch card + two
  captions (personal LinkedIn + company LinkedIn).
- **Do not regenerate these images or rewrite the captions** — Lance
  is scheduling them as-is.

### ASB meeting prep complete
- `audits/asb-hawaii-2026-05/` audit fully populated. Sections 4, 5,
  6 hand-filled with real Claude API data + live competitor scans.
  PDF regenerated at 652KB.
- `content/asb-mvnp-meeting-prep.md` — Q&A prep for Mark Cunningham
  and James Malins. Eight major Q&A categories.
- `content/asb-mvnp-onepager.md` — leave-behind document.
- `content/asb-mvnp-preread-email.md` — email to send 24h before the
  meeting.
- **Meeting is May 18.** Do not modify these without checking the
  meeting prep doc for the specific talking-track they support.

### Hawaii Theatre customer (only paying customer)
- Bug fixes shipped in `dashboard/`. Menu redirect loop, team-invite
  UI, branded error page, auto-onboarding, `/admin/free-check` 1000-
  subrequest cap fix.
- Migrations 0067 and 0068 applied to production D1.
- **Do not touch Hawaii Theatre's user records or roadmap data**
  without checking the full incident audit at
  `content/audit-dashboard-incident-2026-05-07.md`.

### Drip campaign infrastructure (in `/Users/lanceroylo/Desktop/neverranked-outreach/` — separate repo)
- A/B prompt-variant infrastructure live (lib/generator.js, scripts/
  generate.js, scripts/ab-stats.js).
- 124 active-window backfills + 32 stuck-prospect recoveries done.
- Touch 2 cron will fire tomorrow (Sunday 4am) for the 32 unblocked
  prospects.
- **Do not modify the drip cron, daily limits, or backfill scripts**
  without coordinating.

### Strategy + content shipped this week
- `content/strategy/moat-research-2026-05.md` — 15-moat taxonomy
- `content/leaderboards/` — Hawaii community banking, law firms,
  boutique hotels (markdown + JSON sidecars)
- `content/prompt-corpus/` — Hawaii community banking + real estate
- `content/agent-readiness/` — standard + 3 vertical templates
- `content/llms-txt/` — standard + 2 vertical templates
- `content/compliance-schema/` — financial services
- `content/certification/` — program overview + MVNP cohort offer
- `content/sla/` — citation floor guarantee + founder continuity
- `content/engine-changelog/` — scaffold (no real entries yet, gated
  on 4+ weeks of measured data — DO NOT publish fabricated entries)
- `reports/state-of-aeo-hawaii-2026/` — annual report draft (not yet
  published, gated on sample size growing to 12+ Hawaii sites)
- `profile/` — 17 brand authority profile pages live at
  neverranked.com/profile/<slug>/
- `schemas/` — public schema marketplace at
  neverranked.com/schemas/
- `standards/` — methodology, llms-txt, agent-readiness pages live
  at neverranked.com/standards/

## What is pending (safe to pick up)

### Highest leverage right now

1. **Hacker News submission of the MCP launch.** Tuesday afternoon
   ET, after LinkedIn posts have warmed up. Title: "Show HN:
   @neverranked/mcp – AEO scoring and llms.txt audit for AI agents."
   Body: brief framing + link to npm + link to blog post.

2. **MCP server v0.1.2 polish.** The `aeo_scan` tool returns a raw
   "HTTP 422" when the URL is unscannable (e.g., neverranked.com
   itself). Should be a friendlier error message. ~1 hour. Files:
   `mcp-server/src/tools/aeo-scan.ts`. After editing, bump version
   to 0.1.2 in `package.json` AND `server.json`. Lance will need to
   `npm publish` and `mcp-publisher publish` from his terminal.

3. **The 11 unread alerts on Lance's dashboard.** Open
   `app.neverranked.com/alerts` and triage. Could be noise or
   customer-affecting. Files: `dashboard/src/routes/alerts.ts`.

### Strategic builds I deferred

4. **Reddit citation tracker (Tier B).** Lives in
   `tools/reddit-tracker/` and `dashboard/migrations/0067_reddit_threads.sql`.
   Some scaffolding may already exist if a previous window started
   this. Check `git log --oneline -- tools/reddit-tracker/`. Build
   scope: see triggered-builds.md "Reverse-engineer citations" entry.

5. **Public scan persistence to D1 (Tier B).** Migrate `event:scan:*`
   keys from KV (90-day TTL) to a permanent D1 table. Triggered build
   doc at `content/triggered-builds.md`.

6. **Citation accuracy auditing (Tier A #4).** 2-week build. Gated on
   first paying customer with 4+ weeks of citation_runs data. Likely
   premature. Skip unless you have specific design questions to
   answer.

### Open issues, not urgent

7. **Hawaii Theatre data drift.** Ron's user is on slug
   `hawaiitheatre`, historical 13-scan dataset on `hawaii-theatre`.
   Resolve only with Lance + Ron on call. Do NOT auto-merge.

8. **MCP submission to other directories.** Awesome-MCP repos,
   Claude Code skill registry, etc. Check
   `github.com/wong2/awesome-mcp-servers` and submit a PR.

## Files / directories to avoid touching unless coordinating

These have been actively modified in the last 24h and changes from
two windows could collide:

- `mcp-server/` — just published; do not version-bump or edit src
  without coordinating
- `dashboard/src/index.ts` — just patched for /admin/free-check
- `dashboard/migrations/` — 0067 (Reddit), 0068 (competitor diff)
  applied; do not renumber
- `audits/asb-hawaii-2026-05/` — meeting-grade; do not regenerate
- `content/asb-mvnp-*` — meeting prep, lock down
- `content/triggered-builds.md` — actively curated, append-only
- `social/posts/` — both posts queued for scheduling

Safe zones (pick anything new here):

- `tools/` — new tools welcome (just don't redo Reddit if scaffolded)
- `scripts/` — new automation welcome (existing scripts stable)
- `content/blog/` — new posts welcome (publish-pending)
- `content/strategy/` — research docs welcome
- `content/case-studies/` — placeholder space, can build out

## Coordination protocol

1. **Always `git pull` before starting any change.** Both windows
   push to `main`.
2. **Commit and push frequently.** Small commits beat conflict
   resolution.
3. **If you edit a shared file, mention it in the commit message
   (e.g. "MIGHT CONFLICT WITH OTHER WINDOW").** Helps the other
   window know to check before overwriting.
4. **If you publish anything externally** (npm, the registry,
   social), update this handoff file with what you did.
5. **Do not regenerate dist/ or any build artifact.** They live in
   .gitignore on a per-package basis. Source-only commits are
   correct.
6. **The user is HST and works overnight.** Do not assume time-of-
   day. If a task feels time-sensitive, ask before scheduling
   anything.

## State of the May 18 ASB+MVNP meeting

- 9 days out as of writing.
- Audit deck ready (`audits/asb-hawaii-2026-05/audit.pdf`).
- Onepager ready (`content/asb-mvnp-onepager.md`).
- Q&A prep ready (`content/asb-mvnp-meeting-prep.md`).
- Pre-read email drafted, hold for 24h before send.
- Mark Cunningham has not yet confirmed the time.

If you build anything between now and May 18, ask whether it is
meeting-relevant. If yes, surface it in the meeting prep doc as
either a leave-behind or a new Q&A talking-track. If no, it can
ship anytime.

## Open question for Lance

The user is running both windows. If you (other window) need a
decision, write the question into a fresh markdown file at
`content/handoff-questions/<topic>.md` and the user will see it
when they next prompt. Do not block on a decision in chat that the
other window cannot see.

---

## Update from the other window — 2026-05-10 early HST

The other Claude window shipped a five-phase build of public State of
AEO infrastructure. Summary so the next session does not collide:

### Phase A — AEO-niche source-type classifier (commit 09b1a76)
- 4 new types in `tools/citation-gap/src/source-types.mjs`:
  `seo-publication`, `aeo-platform`, `aeo-services-agency`,
  `google-ai-infra`.
- Matching `SOURCE_BRIEF_LIBRARY` entries in `tools/citation-gap/src/brief.mjs`.
- Tests updated: 56/56 passing.

### Phase B — Public /state-of-aeo hub (commit 244ba3b)
- New public surface at `neverranked.com/state-of-aeo/`.
- Hub + per-report pages + PDF downloads. Two reports linked:
  weekly 2026-05-10 + Hawaii 2026 edition (now badged "Working draft"
  per the publication-threshold gating decision in the original handoff).
- Generator: `scripts/state-of-aeo-publish.mjs`. Idempotent, scans
  `reports/state-of-aeo*/` and emits `state-of-aeo/<slug>/index.html`,
  `state-of-aeo/<slug>.pdf`, and `state-of-aeo/latest.json`.
- `state-of-aeo` added to the `DIRS` list in `scripts/build.sh`.
- Sitemap entries added.

### Phase C — Weekly cron (commit 1453525)
- New workflow: `.github/workflows/weekly-state-of-aeo.yml`.
- Fires Mondays 04:00 UTC (Sunday 6pm HST) so the regenerated
  `latest.json` is in place ~2 hours before the dashboard 06:00 UTC
  Monday digest cron reads it.
- Manual run already verified green: 1m 2s, no errors.
- The bot author "neverranked-bot" commits regenerated reports back
  to main, which auto-triggers the deploy workflow.

### Phase D — State of AEO headline in weekly digest (commit 64efbb9)
- New module: `dashboard/src/state-of-aeo.ts` — fetches
  `https://neverranked.com/state-of-aeo/latest.json` with Cache API
  TTL, renders an email-safe HTML block.
- `dashboard/src/cron.ts` and `dashboard/src/email.ts` updated to
  thread the payload through `sendDigestEmail` and into
  `buildDigestHtml`. Block renders above the CTA. Skipped on
  white-label sends.
- Drafts are excluded from being chosen as `latest.json`, so the
  digest never headlines a working-draft edition.

### Phase E — Audit and timing fix (commits 8b92831 + draft tagging)
- Caught a timing bug: regen was scheduled too late to land before
  the Monday digest. Moved to 04:00 UTC.
- Hawaii 2026 edition now shows a "Draft" badge on the hub and a
  full banner at the top of the report page (`status: working draft`
  in front matter triggers it).

### Files that should be considered "in flight" until the next sync
- `scripts/state-of-aeo-publish.mjs` (new)
- `state-of-aeo/` (new public directory)
- `dashboard/src/state-of-aeo.ts` (new)
- `dashboard/src/cron.ts`, `dashboard/src/email.ts` (digest wiring,
  light edits)
- `.github/workflows/weekly-state-of-aeo.yml` (new)
- `.github/workflows/deploy.yml` (added `standards/**` and
  `state-of-aeo/**` to trigger paths)
- `reports/state-of-aeo-hawaii-2026/state-of-aeo-hawaii-2026.md`
  (one-word voice fix: "highest-leverage" -> "highest-impact")

### What the other window should NOT touch without coordinating
- The publish script auto-discovers anything under
  `reports/state-of-aeo*/`. New report markdown there will appear on
  the public hub at the next workflow run.
- `state-of-aeo/latest.json` is regenerated by the publish script
  every run; do not hand-edit.
- Workflow cron schedule (`0 4 * * 1`) is calibrated to lead the
  dashboard 06:00 UTC Monday digest. Moving it later would push the
  Monday digest back a week.

---

End of handoff. Good luck.
